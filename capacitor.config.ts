import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.course.firecandc',
  appName: 'firecandc',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
